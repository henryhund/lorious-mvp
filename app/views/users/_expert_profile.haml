.avatar
  %img{src: @user.avatar_url, width: '75px'}

.name
  %h1= @user

.website
  %p= @user.website

.bio
  %p= @user.bio

.send-message
  = link_to "Message", new_expert_message_url(@user)

.book-appointment
  = link_to "Request Appt.", new_user_appointment_url(@user.username)

.social-media-profiles
  // SOCIAL MEDIA PROFILES

.rating
  // EXPERT RATING

.conversations
  %p= "Number of conversations: #{@user.number_of_conversations}"

.hourly-rate
  %p= number_to_currency(@user.hourly_rate)

.tags
  %ul
    - @user.tags.each do |tag|
      %li= tag

.can-also-help-with
  // EXTRA

.reviews
  // REVIEWS
